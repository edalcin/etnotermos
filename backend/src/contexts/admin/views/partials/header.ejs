<header class="bg-forest-700 text-white shadow-lg">
  <div class="container mx-auto px-4">
    <nav class="flex items-center justify-between py-4">
      <!-- Logo and Brand -->
      <div class="flex items-center space-x-4">
        <a href="/" class="flex items-center space-x-2 hover:opacity-90 transition-opacity">
          <span class="text-3xl">🔒</span>
          <div class="flex flex-col">
            <span class="text-xl font-bold">EtnoTermos Admin</span>
            <span class="text-xs text-forest-100">Gerenciamento de Vocabulário</span>
          </div>
        </a>
      </div>

      <!-- Navigation Links -->
      <div class="hidden md:flex items-center space-x-6">
        <a href="/" class="hover:text-forest-200 transition-colors">Dashboard</a>
        <a href="/terms" class="hover:text-forest-200 transition-colors">Termos</a>
        <a href="/relationships" class="hover:text-forest-200 transition-colors">Relacionamentos</a>
        <a href="/collections" class="hover:text-forest-200 transition-colors">Coleções</a>
        <a href="/audit" class="hover:text-forest-200 transition-colors">Auditoria</a>
        <a href="http://localhost:4000" target="_blank" class="text-forest-200 hover:text-white transition-colors">
          Ver Interface Pública →
        </a>
      </div>

      <!-- Mobile Menu Button -->
      <button
        class="md:hidden text-white"
        x-data="{ mobileMenuOpen: false }"
        @click="mobileMenuOpen = !mobileMenuOpen"
      >
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path x-show="!mobileMenuOpen" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          <path x-show="mobileMenuOpen" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
        </svg>
      </button>
    </nav>

    <!-- Mobile Menu -->
    <div
      x-data="{ open: false }"
      x-show="open"
      @click.outside="open = false"
      class="md:hidden py-4 border-t border-forest-600"
    >
      <a href="/" class="block py-2 hover:text-forest-200 transition-colors">Dashboard</a>
      <a href="/terms" class="block py-2 hover:text-forest-200 transition-colors">Termos</a>
      <a href="/relationships" class="block py-2 hover:text-forest-200 transition-colors">Relacionamentos</a>
      <a href="/collections" class="block py-2 hover:text-forest-200 transition-colors">Coleções</a>
      <a href="/audit" class="block py-2 hover:text-forest-200 transition-colors">Auditoria</a>
    </div>
  </div>
</header>
